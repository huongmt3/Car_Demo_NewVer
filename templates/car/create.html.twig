{% extends 'base.html.twig' %}

{% block title %}Create Car{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create New Car</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary" type="submit">Create</button>
        {{ form_end(form) }}

        <a href="{{ path('car_index') }}" class="btn btn-secondary mt-3">Back to Car List</a>
    </div>
{% endblock %}
